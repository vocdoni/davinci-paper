% !TeX root = ../build/main.tex

The ballot protocol provides a unified and parametric way to represent a wide range of voting systems within \davinci. Instead of designing a separate circuit for each voting rule, the protocol defines ballots as fixed-length arrays of integers, subject to a small set of configurable parameters. These parameters constrain the values that can appear in each field of the ballot and the aggregate properties of the ballot as a whole. By adjusting these parameters, the same circuit can implement approval voting, ranking, quadratic voting, multiple choice, and many other schemes.

This abstraction has two key advantages. First, it allows a broad variety of voting systems to be supported with a minimal circuit design, avoiding conditional logic that would otherwise increase constraint counts. Second, it provides a common interface for tallying, since all ballots are aggregated into a single results array regardless of the voting mode.

\paragraph{Definition of parameters.} 
Each ballot is defined as an array of integer values subject to the following configurable parameters:
\begin{itemize}
	\item \numfields: the maximum number of fields (i.e., options) in the ballot.
	\item \minvalue, \maxvalue: the allowable range for each field value.
	\item \uniquevalues: a Boolean flag specifying whether all field values must be distinct (as in ranking systems).
	\item \costexponent: the exponent applied when computing the cost of casting votes in a field; this parameter enables quadratic or higher-order voting rules.
	\item \minvaluesum, \maxvaluesum: bounds on the total sum of all field values in a ballot (e.g., to enforce a budget of credits).
\end{itemize}

\begin{figure}[H]
	\centering
	\fbox{
		\includegraphics[scale = 0.4, draft = false]{\figs/ballot-variables.png}}
\end{figure}

\paragraph{General mechanism.}
A ballot is valid if and only if all the above constraints are satisfied. Invalid ballots are rejected at the circuit level and do not contribute to the tally. Valid ballots are aggregated into a results array, where each entry is the sum of all votes cast for the corresponding field across all voters. This unified representation ensures that tallying can be performed efficiently, independently of the chosen voting system, and that verification inside zero-knowledge circuits remains simple. 

Figure~\ref{fig:ballot-modes} illustrates several concrete instantiations of this protocol, showing how different voting systems can be realized by selecting appropriate parameter values.
%The examples in Table~\ref{tab:ballot-modes} are not exhaustive. Other systems, such as cumulative voting, weighted scoring, or variations of ranked-choice voting, can be instantiated within the same framework by adjusting the protocol parameters accordingly. This flexibility illustrates the expressiveness of the ballot protocol and its suitability as a general-purpose abstraction for representing diverse voting rules in a unified way.

\martai{It is called Ballot Mode because we define some modes (like quadratic, etc.) that can be chosen by the organizer. Essentially, each of this modes has a special configuration, but in order to make it easier, the organizer only needs to choose which of the modes (configs) to apply.}

%\begin{landscape}
\begin{figure}[h]
	\centerline{\includegraphics[width=\textwidth,draft=false]{\figs/ballot-protocol-table.png}}
	\caption{Table showing various voting models using different ballot configurations.}
	\label{fig:ballot-modes}
\end{figure}
%\end{landscape}

\martai{Mention the weight! Which is maxValueSum!}